
const SingleTransactionPage = ({ params }) => {
    const { id } = params;
    return (
        <div>
            {id} SingleTransactionPage
        </div>
    )
}

export default SingleTransactionPage;